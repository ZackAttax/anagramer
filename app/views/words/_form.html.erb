<%= form_with(model: word) do |form| %>
  <% if word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(word.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
        <% word.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :orginal_word %>
    <%= form.text_field :orginal_word %>
  </div>

  <div class="field">
    <%= form.label :sorted_word %>
    <%= form.text_field :sorted_word %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
